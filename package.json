{
  "name": "curl-request",
  "version": "1.0.0",
  "description": "curl request manual",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "curl",
    "request",
    "curl-request"
  ],
  "author": "lcn",
  "license": "ISC",
  "dependencies": {
    "node-libcurl": "^2.3.4",
    "querystring": "^0.2.1"
  }
}
